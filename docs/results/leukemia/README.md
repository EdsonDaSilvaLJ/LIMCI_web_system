# Resultados do módulo de LLA-B

Esta pasta armazena somente artefatos textuais e pequenos produzidos pelo
experimento, como:

- `inference_config.json`;
- `test_metrics.json`;
- `evaluation_summary.json`.

O modelo `.keras`, os pesos `.h5`, o dataset e imagens individuais não devem
ser versionados neste diretório.
